{
    "scripts": {
        "start": "mkdir -p dist && node main.js ./data/*.csv > dist/tocfl.csv && node main.js --tabs ./data/*.csv > dist/tocfl.tsv"
    },
    "devDependencies": {
        "@types/node": "^10.5.8"
    },
    "dependencies": {
        "parse-cc-cedict": "^2.1.0"
    }
}
